// Placeholder for cross-package shared test fixtures (none yet).
// Reserved for Plans 02–04 which will add Zod-schema test factories
// (env, auth DTOs, consent payload). Keep this file present so
// `vitest.config.ts setupFiles` resolves without a runtime require error.
export {};
