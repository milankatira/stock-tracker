---
phase: 06-news-feed-sentiment
plan: 02
slug: sentiment-vector
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - apps/api/src/ai/ai.service.ts
  - apps/api/src/ai/ai.types.ts
  - apps/api/src/ai/ai.module.ts
  - apps/api/src/news/news.repository.ts
  - apps/api/src/news/news.service.ts
  - apps/api/src/news/jobs/news-poll.processor.ts
  - apps/api/src/news/jobs/embed-classify.processor.ts
  - apps/api/src/sentiment/sentiment.module.ts
  - apps/api/src/sentiment/sentiment.service.ts
  - apps/api/src/sentiment/sentiment.types.ts
  - apps/api/src/sentiment/aggregator.ts
  - apps/api/src/sentiment/aggregator.spec.ts
  - apps/api/src/sentiment/pillar-publisher.ts
  - apps/api/src/sentiment/pillar-publisher.spec.ts
  - apps/api/src/scoring/score-input-builder.ts
  - apps/api/src/news/news.controller.ts
  - apps/api/test/smoke/sentiment.smoke.ts
  - apps/api/test/smoke/embedding.smoke.ts
  - apps/api/test/integration/semantic-search.spec.ts
  - apps/api/test/integration/score-input-builder.spec.ts
  - apps/api/test/integration/news-persist-compliance.spec.ts
  - apps/web/src/app/stock/[ticker]/components/NewsFeed.tsx
  - apps/web/src/app/stock/[ticker]/components/NewsFeed.test.tsx
  - apps/web/src/app/stock/[ticker]/components/SentimentBadge.tsx
  - apps/web/src/app/stock/[ticker]/components/RelativeTime.tsx
  - apps/web/src/app/stock/[ticker]/page.tsx
  - packages/shared/src/news.ts
autonomous: true
requirements:
  - NEWS-02
  - NEWS-03
  - NEWS-04
must_haves:
  truths:
    - "Every News doc, after the embed-classify pipeline runs, has embedding.length === 768"
    - "Every News doc, after classify completes, has sentiment ∈ {POSITIVE, NEGATIVE, NEUTRAL} and a sentimentRationale that contains NO forbidden verbs"
    - "AIService.classifySentiment() and AIService.embedForStorage() are the ONLY entry points to Gemini for this phase; both go through the ComplianceInterceptor (sentiment) / private gemini client (embedding)"
    - "Atlas $vectorSearch returns hits filtered by instrumentMentions + publishedAt window"
    - "aggregateSentimentPillar([]) === null (so ScoringModule falls back to neutral)"
    - "aggregateSentimentPillar([3 POSITIVE]) > 5 and aggregateSentimentPillar([3 NEGATIVE]) < 5"
    - "EOD recompute consumes ScoreInput.sentimentPillar.fromNews when non-null; uses Phase-3 neutral fallback when null"
    - "Stock report page renders <NewsFeed /> with 10 most-recent items, sentiment badge per item, source + relative-time, target='_blank' rel='noopener noreferrer'"
    - "A sentiment shift of ≥ 0.5 pillar points enqueues a score-recompute:{ticker} job; shifts < 0.5 are absorbed by the nightly EOD"
  artifacts:
    - path: "apps/api/src/ai/ai.service.ts"
      provides: "classifySentiment() + embedForStorage() + embedForQuery() — single Gemini chokepoint"
      contains: "classifySentiment"
    - path: "apps/api/src/sentiment/aggregator.ts"
      provides: "Pure aggregateSentimentPillar(items, asOf): number | null"
      exports: ["aggregateSentimentPillar"]
    - path: "apps/api/src/sentiment/pillar-publisher.ts"
      provides: "Writes sentimentPillar.fromNews into ScoreInput; emits selective recompute trigger"
      exports: ["publishSentimentPillar"]
    - path: "apps/api/src/scoring/score-input-builder.ts"
      provides: "Phase-3 input builder, AMENDED to read sentimentPillar.fromNews when non-null"
      contains: "sentimentPillar"
    - path: "apps/web/src/app/stock/[ticker]/components/NewsFeed.tsx"
      provides: "RSC news feed with sentiment badge per item"
      contains: "SENTIMENT_BADGE"
    - path: "packages/shared/src/news.ts"
      provides: "NewsItem + SentimentLabel types shared between API and web"
      exports: ["NewsItem", "SentimentLabel"]
  key_links:
    - from: "apps/api/src/news/jobs/embed-classify.processor.ts"
      to: "apps/api/src/ai/ai.service.ts"
      via: "DI — embedForStorage + classifySentiment"
      pattern: "aiService\\.(embedForStorage|classifySentiment)"
    - from: "apps/api/src/ai/ai.service.ts"
      to: "apps/api/src/compliance/compliance.interceptor.ts"
      via: "@UseInterceptors(ComplianceInterceptor) on classifySentiment"
      pattern: "ComplianceInterceptor"
    - from: "apps/api/src/sentiment/pillar-publisher.ts"
      to: "apps/api/src/sentiment/aggregator.ts"
      via: "calls aggregateSentimentPillar() with last-30d news for the instrument"
      pattern: "aggregateSentimentPillar"
    - from: "apps/api/src/scoring/score-input-builder.ts"
      to: "apps/api/src/sentiment/pillar-publisher.ts"
      via: "reads cached pillar value or recomputes via aggregator"
      pattern: "sentimentPillar\\.fromNews"
    - from: "apps/web/src/app/stock/[ticker]/page.tsx"
      to: "apps/web/src/app/stock/[ticker]/components/NewsFeed.tsx"
      via: "RSC composition; server-fetches /stocks/:ticker/news"
      pattern: "<NewsFeed"
---

<objective>
Complete the Phase-6 pipeline by adding embeddings + sentiment classification + Atlas Vector Search reads + the sentiment-pillar feedback into ScoringModule + the frontend news feed on the stock report.

Builds on Plan 01: News docs exist with `classificationStatus: 'pending'` and `embedding: undefined`. This plan adds the `embed-classify` BullMQ child job that runs *after* `process-article` from Plan 01, plus the aggregator/publisher chain that feeds ScoringModule, plus the `<NewsFeed />` component on the stock report.

Purpose: Deliver NEWS-02 (sentiment chip per item, compliance-clean), NEWS-03 (vectors populated + semantic-search query path), NEWS-04 (pillar wired into scoring).

Output: AI module gains `classifySentiment` (Compliance-wrapped) + `embedForStorage` (768 dims) + `embedForQuery`. Sentiment module: aggregator (TDD, pure) + pillar publisher + selective recompute trigger. Scoring score-input-builder reads pillar when non-null. Frontend renders the news feed with sentiment badges. Smoke tests against live Gemini confirm embedding-dim + classification round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/phases/06-news-feed-sentiment/06-RESEARCH.md
@.planning/phases/06-news-feed-sentiment/06-01-PLAN.md
@.planning/phases/06-news-feed-sentiment/06-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 + Phase 3/4 that this plan consumes. -->

From apps/api/src/news/vector/vector-index.spec.ts (Plan 01):
```typescript
export const NEWS_VECTOR_INDEX_NAME = 'news_embedding_idx';
export const NEWS_EMBEDDING_DIM = 768;  // SINGLE SOURCE OF TRUTH
```

From apps/api/src/news/news.repository.ts (Plan 01 stub; this plan completes):
```typescript
// Plan 01 left these:
findByInstrument(instrumentId: string, limit: number): Promise<News[]>;
insertPending(item: ParsedNewsItem): Promise<News | null>;
updateClassification(id, fields): Promise<void>;
updateEmbedding(id, vec, model, ver): Promise<void>;
// Plan 02 fills:
semanticSearch(opts: VectorSearchOpts): Promise<News[]>;  // see Example 2 in 06-RESEARCH.md
findRecentClassifiedForInstrument(instrumentId: string, sinceDays: number): Promise<News[]>;
```

From apps/api/src/ai/ai.module.ts (Phase 4):
```typescript
// EXISTING: private gemini client lives here; only AIService is exported.
// This plan ADDS classifySentiment + embedForStorage + embedForQuery methods to AIService.
// The ComplianceInterceptor from Phase 4 is reused unchanged.
```

From apps/api/src/scoring/* (Phase 3):
```typescript
// ScoreInput shape (Phase 3):
interface ScoreInput {
  // ...other pillars
  sentimentPillar: {
    fromNews: number | null;   // [0..10] or null → ScoringModule uses neutral fallback
    coverage: { itemCount: number; lookbackDays: number };
  };
}
// scoreStock() consumes input.sentimentPillar.fromNews when non-null.
```

From apps/api/src/compliance/compliance.interceptor.ts (Phase 4):
```typescript
// Strips forbidden verbs (buy/sell/recommend/target/should/must/will rise/will fall) from any string
// in the response payload. Reused unchanged.
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: AIService embeddings + classify methods; embed-classify BullMQ child job; semanticSearch repo method; live-Gemini smoke tests</name>
  <files>
    apps/api/src/ai/ai.service.ts,
    apps/api/src/ai/ai.types.ts,
    apps/api/src/news/news.repository.ts,
    apps/api/src/news/news.service.ts,
    apps/api/src/news/jobs/news-poll.processor.ts,
    apps/api/src/news/jobs/embed-classify.processor.ts,
    apps/api/test/smoke/sentiment.smoke.ts,
    apps/api/test/smoke/embedding.smoke.ts,
    apps/api/test/integration/semantic-search.spec.ts,
    apps/api/test/integration/news-persist-compliance.spec.ts
  </files>
  <action>
**AIService additions (the chokepoint).** All three new methods live inside the existing `AIService` (`apps/api/src/ai/ai.service.ts`); the raw `gemini.client.ts` stays private. NO new file outside `ai/` may import `@google/genai` — enforced by the existing lint rule from Phase 4.

  1. `embedForStorage(text: string): Promise<number[]>`:
     - Model `gemini-embedding-001`, `taskType: 'RETRIEVAL_DOCUMENT'`, `outputDimensionality: NEWS_EMBEDDING_DIM` (imported from `news/vector/vector-index.spec.ts`).
     - Asserts `vec.length === NEWS_EMBEDDING_DIM`; throws on mismatch — second line of defense after the boot index assertion.
  2. `embedForQuery(text: string): Promise<number[]>`:
     - Same model + dim; `taskType: 'RETRIEVAL_QUERY'`. Used by Phase 7 (Ask FinSight) — provide it now so Phase 7 doesn't have to touch AI module.
  3. `classifySentiment(text: string): Promise<SentimentResult>`:
     - Model `gemini-2.5-flash-lite`, `temperature: 0.0`, `responseMimeType: 'application/json'`, `responseSchema: SENTIMENT_SCHEMA` (see 06-RESEARCH.md Example 3).
     - System instruction includes the HARD constraints: `POSITIVE|NEGATIVE|NEUTRAL`, `rationaleOneLine` ≤ 20 words, NEVER use forbidden verbs, "analysis not advice".
     - `@UseInterceptors(ComplianceInterceptor)` on the method (or wrap at the service-class level — match the Phase 4 pattern already in `ai.module.ts`).
     - Returns `{ sentiment, confidence, rationaleOneLine }` post-interceptor.

Add `ai.types.ts` exporting `SentimentLabel = 'POSITIVE' | 'NEGATIVE' | 'NEUTRAL'`, `SentimentResult`, and re-export `NEWS_EMBEDDING_DIM` as a convenience.

**NewsRepository.semanticSearch** — implement per 06-RESEARCH.md Example 2:
  - Stage: `$vectorSearch` with `index: NEWS_VECTOR_INDEX_NAME`, `path: 'embedding'`, `queryVector`, `numCandidates: Math.min(opts.numCandidates ?? 200, 500)` (DoS cap, T-06-13), `limit: Math.min(opts.limit ?? 10, 50)`, `filter: { instrumentMentions: opts.instrumentId, publishedAt: { $gte: since } }`.
  - Project `score: { $meta: 'vectorSearchScore' }` so callers can rank.
  - Also implement `findRecentClassifiedForInstrument(instrumentId, sinceDays)` for the aggregator (no vector ops — straight Mongo query on `instrumentMentions` + `publishedAt` + `classificationStatus: 'classified'`).

**embed-classify processor** — new BullMQ child job in `news-poll` queue (or a sibling queue `news-embed-classify` — pick sibling queue so per-article rate limits stay tight). Triggered by Plan 01's `process-article` AT THE END (modify `news-poll.processor.ts` to enqueue `embed-classify:{newsId}` instead of leaving the doc as `pending` forever). Pipeline:
  1. Load the doc.
  2. If `embedding == null`: `vec = await aiService.embedForStorage(${title}. ${description ?? ''})`; `await repo.updateEmbedding(id, vec, 'gemini-embedding-001', '1')`.
  3. If `sentiment == null`: `result = await aiService.classifySentiment(${title}. ${description ?? ''})`; `await repo.updateClassification(id, { sentiment: result.sentiment, sentimentConfidence: result.confidence, sentimentRationale: result.rationaleOneLine || null, classifierModel: 'gemini-2.5-flash-lite', classifierVersion: '1', classificationStatus: 'classified' })`.
  4. On Gemini 429/transient: BullMQ retry with exponential backoff (use existing repo defaults: `attempts: 5, backoff: { type: 'exponential', delay: 2000 }`).
  5. On terminal failure after retries: set `classificationStatus: 'failed'`, log ERROR, do not block the queue.
  6. Per-article cache key for re-classify: `sentiment:${articleId}:gemini-2.5-flash-lite:1`. Bumping `classifierVersion` invalidates the cache and triggers re-classify on next pickup (Pitfall 5).
  7. After successful classify, emit a domain event `news.classified` (event-emitter or direct method call into `SentimentService.onArticleClassified(newsId, instrumentMentions)` — Task 2 wires the listener).

BullMQ knobs: `concurrency: 4`, `rateLimit: { max: 60, duration: 60_000 }` (60 RPM, conservative — research Pitfall 7). Per-job timeout: 30 s.

**Live-Gemini smoke tests** (gated behind `RUN_LIVE_SMOKE=1` env so CI doesn't burn credits on every PR — match existing pattern from Phase 4):
  - `embedding.smoke.ts`: `await aiService.embedForStorage('Tata Motors profit jumps 30%')` → assert `length === 768`, `vec.every(n => typeof n === 'number')`.
  - `sentiment.smoke.ts`: `await aiService.classifySentiment('Tata Motors Q4 profit jumps 30% on EV strength')` → assert `sentiment === 'POSITIVE'` AND `confidence >= 0.6` AND `rationaleOneLine` contains no forbidden token.

**Integration tests**:
  - `news-persist-compliance.spec.ts`: synthesise a Gemini response with `rationaleOneLine: 'Strong BUY signal on Tata'` (mock `aiService.classifySentiment` → bypass interceptor by injecting raw response into the interceptor's input, OR mock at gemini client level), run the pipeline, assert the persisted `News.sentimentRationale` has 'BUY' stripped per Phase 4's interceptor. This pins NEWS-02's compliance requirement.
  - `semantic-search.spec.ts`: seed 5 News docs with hand-crafted vectors (or against staging Atlas if available — gate behind `RUN_INTEGRATION=1`), call `repo.semanticSearch({ queryVector, instrumentId: 'INFY', sinceDays: 30 })`, assert hits are filtered + ordered by `$vectorSearchScore`.

DO NOT touch the Phase-4 `ComplianceInterceptor` itself — reuse as-is.
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- ai.service news-persist-compliance semantic-search</automated>
    Live smoke (manual gate): `RUN_LIVE_SMOKE=1 pnpm test:smoke -- sentiment.smoke embedding.smoke`.
  </verify>
  <done>
    - `AIService.classifySentiment` + `embedForStorage` + `embedForQuery` exist; all in `ai/`; none reachable without going through AIService.
    - `embed-classify` processor runs after `process-article` and populates `embedding` (length 768) + sentiment fields.
    - `News.sentimentRationale` is empirically stripped of forbidden verbs (compliance integration test green).
    - `NewsRepository.semanticSearch` works against Atlas with `instrumentMentions` + `publishedAt` filter.
    - Live-Gemini smoke tests green when explicitly invoked.
    - `classifierVersion` field bump invalidates per-article cache and triggers re-classify (Pitfall 5 ack).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SentimentService + pure aggregator (TDD) + pillar-publisher + ScoreInput wire-up + selective recompute trigger</name>
  <files>
    apps/api/src/sentiment/sentiment.module.ts,
    apps/api/src/sentiment/sentiment.service.ts,
    apps/api/src/sentiment/sentiment.types.ts,
    apps/api/src/sentiment/aggregator.ts,
    apps/api/src/sentiment/aggregator.spec.ts,
    apps/api/src/sentiment/pillar-publisher.ts,
    apps/api/src/sentiment/pillar-publisher.spec.ts,
    apps/api/src/scoring/score-input-builder.ts,
    apps/api/test/integration/score-input-builder.spec.ts
  </files>
  <behavior>
**aggregator.spec.ts — RED first.** `aggregateSentimentPillar(items, asOf): number | null`:

  - Empty input → `null` (so ScoringModule falls back to Phase-3 neutral). MUST be RED before implementation.
  - `[{POSITIVE, conf: 1, source: 'moneycontrol', publishedAt: asOf - 1h}] × 3` → result `> 5` and `≤ 10`.
  - `[{NEGATIVE, conf: 1, source: 'moneycontrol', publishedAt: asOf - 1h}] × 3` → result `< 5` and `≥ 0`.
  - `[{NEUTRAL, conf: 1, ...}] × 3` → result `=== 5` (within float epsilon).
  - Recency decay: a `POSITIVE` 1 hour old vs. a `POSITIVE` 168 hours old (τ = 168) → identical sign, but the closer one's weight is ~`e` times larger. Test by feeding `[POS@1h, NEG@168h]` → result > 5 (recent positive dominates).
  - Source authority: `[{POS, source: 'bse-announcements', age: 1h}]` vs `[{POS, source: 'newsdata-io', age: 1h}]` → both > 5, but the BSE one closer to 10 (authority 1.2 vs 0.6).
  - Unknown source defaults to authority 0.5 (graceful — not throw).
  - All items with `confidence: 0` → `null` (den === 0 path).
  - Formula matches 06-RESEARCH.md Pattern 4 EXACTLY:
    ```
    w_i = exp(-age_h / 168) * sourceAuthority[source]
    raw = sum(w * polarity * conf) / sum(w)   // [-1, +1]
    pillar = clamp(5 + 5 * raw, 0, 10)
    ```

**pillar-publisher.spec.ts**:
  - Given a recompute call for `instrumentId: 'INFY'`, the publisher fetches recent classified news, calls aggregator, returns `{ value: number | null, coverage: { itemCount, lookbackDays: 30 } }`.
  - When the value is `null` (no qualifying news), the ScoreInput builder must read it as `fromNews: null` so Phase-3 neutral fallback kicks in.
  - When the new value differs from the cached value by `>= 0.5` → enqueue `score-recompute:{ticker}` BullMQ job. When `< 0.5` → no-op. Cached in Redis at `sentiment:pillar:{instrumentId}` with TTL 36h.

**score-input-builder.spec.ts** (integration):
  - Stub `pillarPublisher.computePillar('INFY')` to return `{ value: 7.5, coverage: {...} }` → resulting `ScoreInput.sentimentPillar.fromNews === 7.5`.
  - Stub to return `{ value: null, coverage: {...} }` → `ScoreInput.sentimentPillar.fromNews === null` and `scoreStock(input)` falls back to neutral (verify via Phase-3 fixture).
  </behavior>
  <action>
**aggregator.ts** — pure function, no I/O, no DI. Copy formula from 06-RESEARCH.md Example 6. Source-authority table is exported as `SOURCE_AUTHORITY` const — admin-tuneable (Open Q surface). `TAU_HOURS = 168` exported const. Polarity map exported.

**sentiment.types.ts**:
```typescript
export type SentimentLabel = 'POSITIVE' | 'NEGATIVE' | 'NEUTRAL';
export interface SentimentItem { source: string; sentiment: SentimentLabel; confidence: number; publishedAt: Date; }
export interface PillarResult { value: number | null; coverage: { itemCount: number; lookbackDays: number; }; }
```

**SentimentService**:
  - `onArticleClassified(newsId, instrumentMentions[])` — listener wired from Task 1's domain event. For each instrumentId in mentions, invoke `pillar-publisher.computePillar(instrumentId)` and apply the selective-recompute trigger.
  - `computePillarForInstrument(instrumentId)` — convenience wrapper used by EOD job too.

**pillar-publisher.ts**:
  - `publishSentimentPillar(instrumentId, asOf = now): Promise<PillarResult>`:
    1. Read last-30d classified news via `newsRepo.findRecentClassifiedForInstrument(instrumentId, 30)`.
    2. Map to `SentimentItem[]` and call `aggregateSentimentPillar(items, asOf)`.
    3. Compare `newValue` vs `redis.get('sentiment:pillar:' + instrumentId)`; on `|new - old| >= 0.5` → enqueue `score-recompute:{instrumentId}`.
    4. `redis.set('sentiment:pillar:' + instrumentId, value, EX: 36 * 3600)`.
    5. Return `{ value, coverage: { itemCount: items.length, lookbackDays: 30 } }`.

**ScoreInput builder amendment** (`apps/api/src/scoring/score-input-builder.ts`):
  - Locate the existing builder from Phase 3 (it currently writes `sentimentPillar.fromNews: null` as a neutral fallback per Phase-3 deliverable).
  - REPLACE the neutral fallback with: `const pillar = await pillarPublisher.publishSentimentPillar(instrumentId, asOf); input.sentimentPillar = { fromNews: pillar.value, coverage: pillar.coverage };`
  - This is the load-bearing wire-up for NEWS-04. The Phase-3 contract is preserved: `null` still flows through to ScoringModule's neutral fallback when there's no news.

**Selective recompute trigger**:
  - The score-recompute queue exists from Phase 3 (`eod-recompute` is the parent; per-ticker child jobs already supported). This task enqueues `score-recompute:{instrumentId}` directly when `|Δ pillar| ≥ 0.5`.
  - Constant `RECOMPUTE_THRESHOLD = 0.5` exported for testability (Assumption A9 — admin-tuneable).

Wire `SentimentModule` into `AppModule`. Wire the `news.classified` event listener so Task 1's emit triggers Task 2's pillar update.
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- aggregator.spec pillar-publisher.spec score-input-builder.spec</automated>
    Aggregator tests RED first (empty input case + neutral-only case), then GREEN after implementation — commit history must reflect this.
  </verify>
  <done>
    - `aggregator.ts` is pure; `aggregator.spec.ts` covers all 8 behavior cases above.
    - `aggregateSentimentPillar([]) === null`.
    - `pillar-publisher.spec.ts` covers the ≥0.5 trigger threshold and the no-op path.
    - `score-input-builder` reads from `pillarPublisher.publishSentimentPillar` and stores `null` when value is null (neutral fallback preserved).
    - On `news.classified` event, `SentimentService` recomputes pillar for every mentioned instrument.
    - `RECOMPUTE_THRESHOLD`, `TAU_HOURS`, `SOURCE_AUTHORITY` exported as constants for admin tuning.
  </done>
</task>

<task type="auto">
  <name>Task 3: Frontend NewsFeed component + sentiment badge + stock report wire-up + shared types</name>
  <files>
    packages/shared/src/news.ts,
    apps/web/src/app/stock/[ticker]/components/NewsFeed.tsx,
    apps/web/src/app/stock/[ticker]/components/NewsFeed.test.tsx,
    apps/web/src/app/stock/[ticker]/components/SentimentBadge.tsx,
    apps/web/src/app/stock/[ticker]/components/RelativeTime.tsx,
    apps/web/src/app/stock/[ticker]/page.tsx,
    apps/api/src/news/news.controller.ts
  </files>
  <action>
**Shared types** (`packages/shared/src/news.ts`):
```typescript
export type SentimentLabel = 'POSITIVE' | 'NEGATIVE' | 'NEUTRAL';
export interface NewsItem {
  id: string;
  title: string;
  url: string;
  source: string;
  publishedAt: string;     // ISO 8601
  sentiment: SentimentLabel | null;
}
```
Re-export from `packages/shared`'s root barrel so both `apps/api` and `apps/web` import from the same module — this is the project monorepo convention (Phase 1).

**API contract** — `NewsController.GET /stocks/:ticker/news` already returns the right shape from Plan 01; verify the field names match the shared `NewsItem` exactly. If `sentiment` was being returned for a `pending` doc, return `null` explicitly (not undefined — JSON serialisation).

**`<SentimentBadge />`** — small presentational component:
```tsx
const STYLES = {
  POSITIVE: 'bg-emerald-100 text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-300',
  NEGATIVE: 'bg-rose-100 text-rose-800 dark:bg-rose-900/30 dark:text-rose-300',
  NEUTRAL:  'bg-zinc-100 text-zinc-700 dark:bg-zinc-800 dark:text-zinc-300',
};
// Renders nothing when sentiment === null (pending) — keeps the layout clean.
```
Tooltip on hover: "Sentiment is analysis based on the headline — not investment advice." (UX polish per dev profile; SEBI angle per Open Q 3.)

**`<RelativeTime />`** — uses `Intl.RelativeTimeFormat` (zero-deps, server-renderable):
  - "2 hours ago", "3 days ago", "just now" (< 60s).
  - `<time dateTime={iso}>` for SEO + accessibility.

**`<NewsFeed />`** (RSC):
```tsx
async function NewsFeed({ ticker }: { ticker: string }) {
  const items = await fetch(`${apiBaseUrl}/stocks/${ticker}/news?limit=10`, { next: { revalidate: 60 } }).then(r => r.json()) as NewsItem[];
  if (items.length === 0) return <EmptyState message="No recent news for this stock." />;
  return (
    <section aria-label="Recent news">
      <h2 className="text-lg font-semibold mb-3">Recent News</h2>
      <ul className="divide-y divide-zinc-200 dark:divide-zinc-800">
        {items.map(item => <NewsItemRow key={item.id} item={item} />)}
      </ul>
      <p className="mt-3 text-xs text-zinc-500">
        Sentiment tags are AI analysis, not investment advice. Past performance is not indicative of future results.
      </p>
    </section>
  );
}
```

**`<NewsItemRow />`**:
  - `<a href={item.url} target="_blank" rel="noopener noreferrer">` — both attributes MANDATORY (T-06-12, T-06-03).
  - Layout: badge (if sentiment) on the left, title + meta on the right.
  - Title is plain string (React escapes — no `dangerouslySetInnerHTML`).
  - `font-medium text-sm leading-snug` on title; `text-xs text-zinc-500` on `{source} · <RelativeTime>`.
  - Hover state: `hover:bg-zinc-50 dark:hover:bg-zinc-900/50` on the `<li>` for a clickable feel.

**NewsFeed.test.tsx** (Vitest + React Testing Library — match existing web test conventions):
  - Renders 10 items.
  - Title appears as text.
  - Each external link has `target="_blank"` AND `rel="noopener noreferrer"`.
  - When `sentiment === null`, no badge renders for that item (no leaked `null` text).
  - Empty array → `EmptyState` renders.
  - Disclaimer text renders.

**Stock report wire-up** (`apps/web/src/app/stock/[ticker]/page.tsx`):
  - Insert `<NewsFeed ticker={ticker} />` between the existing six-card grid and the peer-comparison section (visual placement matching the PRD reference report layout).
  - Wrap in `<Suspense fallback={<NewsFeedSkeleton />}>` so the rest of the report renders while news loads — keeps the <2s render budget for the score+cards.
  - Add a basic `<NewsFeedSkeleton />` (3 placeholder rows with shimmer) inline.

**Compliance footer disclaimer** — the page-level "Analysis not advice" disclaimer from Phase 4 already exists; do NOT duplicate. The news-specific microcopy is the small line inside the section.

DO NOT add a search/filter UI on news — out of scope per Open Q 5.
DO NOT render the news feed on the fund report — Phase 6 scope is stocks only per NEWS-01 + Open Q 5.
  </action>
  <verify>
    <automated>pnpm --filter web test -- NewsFeed.test &amp;&amp; pnpm --filter web build</automated>
    Build verifies the RSC fetch wiring + shared-types import path.
    Manual: `pnpm --filter web dev`, visit `/stock/INFY`, confirm news section renders with sentiment chips and external links have `target="_blank" rel="noopener noreferrer"` in view-source.
  </verify>
  <done>
    - `<NewsFeed />` renders on `/stock/[ticker]` page above-fold-adjacent.
    - Sentiment badges appear for `POSITIVE / NEGATIVE / NEUTRAL`; nothing renders for `null`.
    - External links all carry `target="_blank" rel="noopener noreferrer"`.
    - Shared `NewsItem` type imported from `packages/shared` by both API and web (no drift).
    - Disclaimer microcopy present.
    - Empty state shows on tickers with no classified news (e.g., brand-new instruments).
    - No `dangerouslySetInnerHTML` introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSS/NewsData content → Gemini prompt | Headline text could carry prompt-injection attempts |
| Gemini response → ComplianceInterceptor → DB | Response could contain forbidden verbs or invented numbers |
| User browser → `<a target="_blank">` | Reverse tabnabbing surface |
| Public client → `GET /stocks/:ticker/news` (vector path later) | Untrusted query params; DoS via huge `numCandidates` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | DoS | Atlas `$vectorSearch` with huge `numCandidates` | mitigate | `semanticSearch` clamps `numCandidates` to `min(input, 500)` and `limit` to `min(input, 50)`; public endpoint never forwards user-supplied vector-search params in Phase 6 — Phase 7 will add an authenticated wrapper. |
| T-06-14 | Tampering | Prompt injection in headline → forced label | mitigate | (a) Strict `responseSchema` constrains output to enum + 0..1 + bounded string; (b) ComplianceInterceptor strips forbidden verbs from `rationaleOneLine`; (c) `temperature: 0.0`; (d) headline is wrapped in quoted delimiter in the prompt. |
| T-06-15 | Info Disclosure | Persisted `sentimentRationale` containing "BUY/SELL/recommend" | mitigate | ComplianceInterceptor enforced at the AIService boundary; `news-persist-compliance.spec.ts` proves it for sentiment. |
| T-06-16 | Tampering | Reverse tabnabbing on news click | mitigate | Mandatory `target="_blank" rel="noopener noreferrer"` on every news link in `<NewsItemRow />`; covered by test. |
| T-06-17 | Tampering | Stored XSS via headline with HTML | mitigate | RSC renders text as JSX string children (React escapes); no `dangerouslySetInnerHTML`; lint rule existing. |
| T-06-18 | Tampering | Atlas index dim drift between embed call and index | mitigate | `embedForStorage` asserts `vec.length === NEWS_EMBEDDING_DIM` and the index assertion runs at boot — two-layer defense. |
| T-06-19 | Tampering | Sentiment cache becomes stale after model swap | mitigate | Cache key includes `classifierModel:classifierVersion`; bumping version triggers re-classify (Pitfall 5). |
| T-06-20 | DoS | Embedding rate limit during backfill kills queue | mitigate | BullMQ `concurrency: 4` + `rateLimit: { max: 60, duration: 60_000 }` on `embed-classify`; exponential backoff on 429. |
| T-06-21 | Repudiation | "Why did the score change yesterday?" | mitigate | `News.classifierModel + classifierVersion` + `embeddingModel + embeddingVersion` are stored per article; ScoreHistory (Phase 3) already captures pillar value at compute time. |
| T-06-22 | Spoofing | Listener-event spoof via misuse of EventEmitter | accept | Internal-only; no external entry point to the `news.classified` event. |
</threat_model>

<verification>
1. `pnpm --filter api test` — all unit + integration green (aggregator RED→GREEN history present).
2. `pnpm --filter web test && pnpm --filter web build` — web tests green; production build emits the news section in SSR HTML (view-source check).
3. Live-Gemini smoke (manual): `RUN_LIVE_SMOKE=1 pnpm --filter api test:smoke` — both embedding (768 dims) and sentiment (POSITIVE for the canary headline) pass.
4. End-to-end smoke (manual): start API + web locally → trigger one `news-poll:tick` → wait for `embed-classify` to finish → curl `GET /stocks/INFY/news` → confirm at least one item has a non-null `sentiment` → load `/stock/INFY` in browser → sentiment badge visible.
5. EOD recompute integration: with seeded classified news, run `score-input-builder` directly for INFY → confirm `ScoreInput.sentimentPillar.fromNews` is a number, not null; with no news for ticker XYZ → confirm `fromNews: null` and ScoringModule falls back to neutral.
6. Compliance audit: scan persisted `News.sentimentRationale` values for forbidden tokens (`buy|sell|recommend|target`) — zero matches expected.
</verification>

<success_criteria>
- NEWS-02 satisfied: every classified News doc has a `sentiment` chip rendered on the report; the persisted `sentimentRationale` is post-ComplianceInterceptor (proven by `news-persist-compliance.spec.ts`).
- NEWS-03 satisfied: every classified News doc has `embedding.length === 768`; `$vectorSearch` returns instrument+recency-filtered hits in `semantic-search.spec.ts`.
- NEWS-04 satisfied: `ScoreInput.sentimentPillar.fromNews` consumes the aggregator output; `null` preserves Phase-3 neutral fallback; `|Δ pillar| ≥ 0.5` triggers `score-recompute:{ticker}`.
- NEWS-01 fully closed (Plan 01 delivered the data; Plan 02 delivers the UI badge + disclaimer).
- No code outside `apps/api/src/ai/` imports `@google/genai`.
- Live-Gemini smoke tests pass when invoked.
- All Gemini calls go through `AIService`; `ComplianceInterceptor` wraps `classifySentiment`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-news-feed-sentiment/06-02-SUMMARY.md` covering:
- Files created (paths) + the aggregator's TDD commit pattern (RED → GREEN → REFACTOR)
- Decisions: τ = 168h, RECOMPUTE_THRESHOLD = 0.5, SOURCE_AUTHORITY weights (with note that they are admin-tuneable)
- Live-Gemini smoke results (label + dim) — confirm against canary headline.
- Re-classify trigger pattern documented for future model swaps (bump `classifierVersion`).
- Phase 6 complete: NEWS-01..04 all green; Phase 7 (Ask FinSight) unblocked — `embedForQuery` + `semanticSearch` are ready to consume.
- Open questions to surface to orchestrator (carry forward from Plan 01 + add: source-authority weights, τ, threshold — defer tuning to milestone retro).
</output>
