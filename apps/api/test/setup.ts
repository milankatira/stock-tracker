// Placeholder — Plan 01 Task 3 replaces this with the full Wave-0
// mongodb-memory-server + ioredis-mock + Google OAuth mock harness.
// Keeping the file present so vitest.config.ts setupFiles[0] resolves
// during the Task 2 verification phase.
export {};
