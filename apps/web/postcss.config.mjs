// Tailwind v4 PostCSS plugin. NO `tailwind.config.js` — v4 is CSS-first;
// theme tokens live in `src/app/globals.css` under the `@theme` directive.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
